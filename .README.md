## Запуск тестов локально

1. Скопировать .env.example в .env
2. Загрузить хром в докер: `docker pull selenoid/chrome:128.0`
3. Выполнить команду: `docker compose up -d`
4. Просмотреть результаты тестов: `allure serve allure-results`
